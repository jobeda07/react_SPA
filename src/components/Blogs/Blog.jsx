import PropTypes from 'prop-types';
 const Blog = ({blog}) => {
  console.log(blog)
 return (
  <div>
   
  </div>
 );
};

Blog.PropTypes ={
  blog:PropTypes.objects.isRequired
}

export default Blog;